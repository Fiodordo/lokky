"use client";
import{useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import SecurityAssistant from"@/components/SecurityAssistant";import Link from"next/link";
export default function EnglishAssistant(){const[scan,setScan]=useState<any>(null),[loading,setLoading]=useState(true);useEffect(()=>{supabase.auth.getUser().then(({data})=>{if(!data.user){setLoading(false);return}supabase.from("scans").select("id,domain,score,security_headers,ssl_valid,https_redirect").eq("user_id",data.user.id).order("created_at",{ascending:false}).limit(1).maybeSingle().then(({data})=>{setScan(data);setLoading(false)})})},[]);const issues=scan?[...(scan.ssl_valid===false?[{label:"SSL certificate",impact:"The SSL certificate should be checked."}]:[]),...(scan.https_redirect===false?[{label:"HTTPS redirect",impact:"Visitors should be redirected to HTTPS."}]:[]),...Object.entries(scan.security_headers||{}).filter(([,ok])=>!ok).map(([key])=>({label:key,impact:"Missing browser protection."}))]:[];return <div className="ea"><header><div><span>LOKKY ASSISTANT</span><h1>Your security copilot.</h1><p>Ask Lokky directly. It starts from your latest scan and adapts to your stack and context.</p></div><Link href="/en/dashboard">← Back to Overview</Link></header>{loading?<div className="state">Loading your context…</div>:scan?<><div className="context"><small>CURRENT CONTEXT</small><strong>{scan.domain}</strong><b>{scan.score}</b><i>{issues.length} issue{issues.length!==1?"s":""} detected</i></div><SecurityAssistant domain={scan.domain} score={scan.score} issues={issues}/></>:<div className="state"><h2>Run your first scan.</h2><p>Lokky will then answer from your SaaS's real security results.</p><Link href="/en/dashboard/scanner">Scan my SaaS →</Link></div>}<style jsx global>{`.ea{max-width:900px;margin:auto;color:#f8fafc;font-family:Inter,Arial,sans-serif}.ea header{display:flex;justify-content:space-between;align-items:flex-end;gap:25px;margin-bottom:28px}.ea header span{font:600 9px ui-monospace;color:#a855f7;letter-spacing:.14em}.ea h1{font-size:36px;letter-spacing:-.04em;margin:9px 0}.ea header p{max-width:620px;color:#858091;font-size:13px;line-height:1.65;margin:0}.ea header>a{white-space:nowrap;color:#b16cff;text-decoration:none;font-size:11px}.context{display:flex;align-items:center;gap:14px;padding:16px 18px;background:rgba(139,92,246,.06);border:1px solid rgba(139,92,246,.2);border-radius:12px;margin-bottom:22px}.context small{font:600 8px ui-monospace;color:#716b7b}.context strong{font-size:12px}.context b{margin-left:auto;padding:6px 10px;border-radius:7px;background:rgba(168,85,247,.12);color:#c4b5fd;font-size:11px}.context i{color:#716b7b;font-size:10px;font-style:normal}.ea :global(.security-assistant){max-width:none;margin:0}.state{border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.025);border-radius:14px;padding:50px;text-align:center;color:#716b7b}.state h2{color:#e9e6ee;font-size:18px}.state p{font-size:12px}.state a{display:inline-block;margin-top:12px;padding:10px 14px;background:#8b5cf6;color:#fff;border-radius:8px;text-decoration:none;font-size:11px}@media(max-width:650px){.ea header{align-items:flex-start;flex-direction:column}.ea h1{font-size:29px}.context{flex-wrap:wrap}.context b{margin-left:0}}`}</style></div>}
