"use client";

import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
import Link from "next/link";

type Scan = { id: string; domain: string; score: string; created_at: string };
const scoreTone = (s: string | null) => {
  if (s === "A" || s === "B") return { color: "#22c55e", bg: "rgba(34,197,94,.09)", label: "Bon" };
  if (s === "C") return { color: "#f59e0b", bg: "rgba(245,158,11,.09)", label: "À corriger" };
  if (s === "D" || s === "F") return { color: "#ef4444", bg: "rgba(239,68,68,.09)", label: "Risque" };
  return { color: "#8b5cf6", bg: "rgba(139,92,246,.09)", label: "Pas encore scanné" };
};

export default function DashboardHome() {
  const [scans, setScans] = useState<Scan[]>([]);
  const [loading, setLoading] = useState(true);
  useEffect(() => { supabase.auth.getUser().then(({ data }) => { if (data.user) loadScans(data.user.id); else setLoading(false); }); }, []);
  async function loadScans(uid: string) { const { data } = await supabase.from("scans").select("id, domain, score, created_at").eq("user_id", uid).order("created_at", { ascending: false }).limit(8); setScans(data ?? []); setLoading(false); }
  const latest = scans[0];
  const tone = scoreTone(latest?.score ?? null);
  const formattedDate = latest ? new Date(latest.created_at).toLocaleDateString("fr-FR", { day: "numeric", month: "short" }) : null;

  return <div className="dash-home">
    <div className="dash-head"><div><div className="dash-kicker">LOKKY / OVERVIEW</div><h1>Ton SaaS, <em>prêt pour la prod ?</em></h1><p>Un endroit simple pour savoir ce qui va bien, ce qui risque de casser et quoi corriger ensuite.</p></div><Link href="/dashboard/scanner" className="dash-primary">+ Scanner un SaaS</Link></div>
    {latest ? <section className="health-card"><div className="health-main"><div className="health-label">DERNIER SCAN <span>• {formattedDate}</span></div><h2>{latest.domain}</h2><p>Dernier résultat de sécurité</p></div><div className="health-score" style={{ color: tone.color, background: tone.bg, borderColor: `${tone.color}55` }}><strong>{latest.score}</strong><span>{tone.label}</span></div><Link href={`/report/${latest.id}`} className="health-link">Voir le rapport →</Link></section> : <section className="empty-hero"><div className="empty-icon">⌁</div><div><div className="dash-kicker">PREMIER PAS</div><h2>Commence par scanner ton SaaS.</h2><p>Colle ton URL et Lokky vérifie les principaux signaux de sécurité visibles depuis l'extérieur.</p></div><Link href="/dashboard/scanner" className="dash-primary">Lancer mon premier scan →</Link></section>}

    <div className="dash-grid"><section className="panel"><div className="panel-head"><div><h3>Scans récents</h3><p>Les dernières vérifications de tes projets</p></div><Link href="/dashboard/history">Tout voir →</Link></div>{loading ? <div className="panel-empty">Chargement...</div> : scans.length === 0 ? <div className="panel-empty">Aucun scan pour le moment.</div> : <div className="scan-list">{scans.slice(0,5).map((scan, i) => { const t = scoreTone(scan.score); return <Link href={`/report/${scan.id}`} className="scan-row" key={`${scan.id}-${i}`}><span className="scan-status" style={{background:t.color}}/><span className="scan-domain">{scan.domain}</span><span className="scan-date">{new Date(scan.created_at).toLocaleDateString("fr-FR",{day:"numeric",month:"short"})}</span><strong style={{color:t.color}}>{scan.score}</strong><span className="scan-arrow">→</span></Link>; })}</div>}</section>
      <section className="panel next-panel"><div className="panel-head"><div><h3>À faire ensuite</h3><p>Le chemin le plus court vers un SaaS plus safe</p></div></div><Link href="/dashboard/scanner" className="next-row"><span className="next-number">01</span><span><strong>Scanner ton SaaS</strong><small>Découvre ton score actuel</small></span><b>→</b></Link><Link href={latest ? `/report/${latest.id}` : "/dashboard/scanner"} className="next-row"><span className="next-number">02</span><span><strong>Comprendre les problèmes</strong><small>{latest ? "Demande à Lokky comment les corriger" : "Ton assistant t'aidera après le scan"}</small></span><b>→</b></Link><Link href={latest ? `/report/${latest.id}` : "/dashboard/scanner"} className="next-row"><span className="next-number">03</span><span><strong>Re-scanner après correction</strong><small>Vérifie que ton score progresse</small></span><b>→</b></Link></section></div>
    <section className="maker-tip"><div className="tip-icon">✦</div><div><span>MAKER TIP</span><h3>Ne cherche pas la perfection avant de shipper.</h3><p>Commence par éliminer les risques visibles. Corrige, re-scanne, puis continue à construire.</p></div><Link href={latest ? `/report/${latest.id}` : "/dashboard/scanner"}>Parler à Lokky →</Link></section>
    <style jsx>{` .dash-home{max-width:1080px;color:#f8fafc;font-family:var(--font-geist-sans),Inter,Arial,sans-serif;letter-spacing:normal}.dash-home *{font-family:inherit}.dash-kicker{font-family:var(--font-geist-mono),ui-monospace,monospace!important;font-weight:600;font-size:10px;letter-spacing:.13em;color:#a855f7;margin-bottom:9px}.dash-head{display:flex;align-items:flex-end;justify-content:space-between;gap:30px;margin-bottom:28px}.dash-head h1{font-size:30px;letter-spacing:-.035em;line-height:1.1;margin:0 0 9px;font-weight:650}.dash-head h1 em{color:#a855f7;font-style:normal}.dash-head p{margin:0;color:#718096;font-size:13px;line-height:1.6;max-width:650px}.dash-primary{display:inline-flex;align-items:center;justify-content:center;white-space:nowrap;text-decoration:none;background:#8b5cf6;color:#fff;border-radius:8px;padding:11px 15px;font-size:12px;font-weight:600;box-shadow:0 10px 30px rgba(139,92,246,.16)}.health-card,.empty-hero{background:linear-gradient(135deg,rgba(139,92,246,.1),rgba(255,255,255,.025));border:1px solid rgba(139,92,246,.18);border-radius:15px;padding:25px;display:flex;align-items:center;gap:24px;margin-bottom:18px}.health-main{flex:1;min-width:0}.health-label{font-family:var(--font-geist-mono),ui-monospace,monospace;font-size:10px;color:#64748b;letter-spacing:.07em}.health-label span{color:#475569}.health-main h2{font-size:20px;font-weight:600;margin:8px 0 4px;letter-spacing:-.02em}.health-main p{font-size:12px;color:#64748b;margin:0}.health-score{width:72px;height:72px;border:1px solid;border-radius:14px;display:flex;flex-direction:column;align-items:center;justify-content:center;flex-shrink:0}.health-score strong{font-size:30px;line-height:1;font-weight:650}.health-score span{font-size:9px;margin-top:4px}.health-link{color:#c4b5fd;text-decoration:none;font-size:11px;white-space:nowrap}.empty-hero{padding:30px}.empty-icon{width:52px;height:52px;border-radius:14px;background:rgba(168,85,247,.1);color:#a855f7;display:grid;place-items:center;font-size:25px;flex-shrink:0}.empty-hero h2{font-size:19px;font-weight:600;margin:0 0 6px}.empty-hero p{font-size:12px;color:#718096;line-height:1.6;max-width:570px;margin:0}.dash-grid{display:grid;grid-template-columns:1.15fr .85fr;gap:18px}.panel{background:#0d0a12;border:1px solid rgba(255,255,255,.07);border-radius:14px;overflow:hidden}.panel-head{padding:19px 20px;border-bottom:1px solid rgba(255,255,255,.06);display:flex;justify-content:space-between;align-items:center}.panel-head h3{font-size:13px;font-weight:600;margin:0 0 4px}.panel-head p{font-size:10px;color:#64748b;margin:0}.panel-head a{font-size:10px;color:#a78bfa;text-decoration:none}.panel-empty{padding:35px 20px;color:#64748b;font-size:12px}.scan-list{width:100%}.scan-row{display:grid;grid-template-columns:8px minmax(0,1fr) 70px 24px 20px;gap:12px;align-items:center;padding:14px 20px;text-decoration:none;border-bottom:1px solid rgba(255,255,255,.045);transition:background .2s;min-width:0}.scan-row:last-child{border-bottom:0}.scan-row:hover,.next-row:hover{background:rgba(255,255,255,.025)}.scan-status{width:7px;height:7px;border-radius:50%;display:block}.scan-domain{color:#e2e8f0;font-size:12px;font-weight:500;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0}.scan-date{font-family:var(--font-geist-mono),ui-monospace,monospace!important;font-size:9px;color:#64748b;white-space:nowrap}.scan-row strong{font-family:var(--font-geist-mono),ui-monospace,monospace!important;font-size:13px;text-align:center;font-weight:700}.scan-arrow{color:#64748b;font-size:12px;text-align:right}.next-row{display:grid;grid-template-columns:30px minmax(0,1fr) 18px;gap:12px;align-items:center;padding:16px 20px;text-decoration:none;border-bottom:1px solid rgba(255,255,255,.045);min-width:0}.next-row:last-child{border-bottom:0}.next-number{font-family:var(--font-geist-mono),ui-monospace,monospace!important;font-size:10px;color:#a855f7}.next-row strong{display:block;color:#e2e8f0;font-size:12px;font-weight:600;margin-bottom:3px}.next-row small{display:block;color:#64748b;font-size:10px;line-height:1.4}.next-row>b{color:#64748b;font-size:12px}.maker-tip{margin-top:18px;padding:20px;border:1px solid rgba(168,85,247,.14);background:rgba(168,85,247,.045);border-radius:14px;display:flex;align-items:center;gap:15px}.tip-icon{color:#c4b5fd;font-size:18px}.maker-tip span{font-family:var(--font-geist-mono),ui-monospace,monospace!important;font-weight:600;font-size:9px;color:#a855f7;letter-spacing:.12em}.maker-tip h3{font-size:12px;font-weight:600;margin:5px 0}.maker-tip p{font-size:10px;color:#64748b;margin:0}.maker-tip a{margin-left:auto;color:#c4b5fd;text-decoration:none;font-size:10px;white-space:nowrap}@media(max-width:800px){.dash-head{align-items:flex-start;flex-direction:column}.dash-head h1{font-size:26px}.health-card,.empty-hero{align-items:flex-start;flex-wrap:wrap}.health-score{margin-left:auto}.health-link{width:100%}.dash-grid{grid-template-columns:1fr}.maker-tip{align-items:flex-start;flex-wrap:wrap}.maker-tip a{margin-left:33px;width:100%}}@media(max-width:520px){.dash-head h1{font-size:23px}.health-card{padding:20px}.scan-row{grid-template-columns:8px minmax(0,1fr) 28px 18px}.scan-date{display:none}.empty-hero{padding:22px}.empty-hero .dash-primary{width:100%}}`}</style>
  </div>;
}
